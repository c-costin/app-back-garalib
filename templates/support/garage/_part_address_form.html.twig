<div class="mb-3">
	<div class="row mb-3">
			<div class="col">
				<label for="number" class="form-label">Numéro</label>
				<input type="number" class="searchbar-input" id="number" name="number" value="{{ address is defined ? address.number : "" }}" required>
			</div>
			<div class="col">
				<label for="type" class="form-label">Type de voie</label>
				<input type="text" class="searchbar-input" id="type" name="type" value="{{ address is defined ? address.type : "" }}" required>
			</div>
			<div class="col">
				<label for="address_name" class="form-label">Nom de voie</label>
				<input type="text" class="searchbar-input" id="address_name" name="address_name" value="{{ address is defined ? address.name : "" }}" required>
			</div>
	</div>
	<div class="row mb-3">
			<div class="col">
				<label for="postalCode" class="form-label">Code postal</label>
				<input type="number" class="searchbar-input" id="postalCode" name="postalCode" value="{{ address is defined ? address.postalCode : "" }}" required>
			</div>
			<div class="col">
				<label for="town" class="form-label">Ville</label>
				<input type="text" class="searchbar-input" id="town" name="town" value="{{ address is defined ? address.town : "" }}" required>
			</div>
	</div>
</div>